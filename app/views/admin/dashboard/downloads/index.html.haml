%h1 Downloads

%ul
  - @downloads.each do |download|
    %li= link_to download.name, admin_dashboard_download_path(download)
= link_to new_admin_dashboard_download_path do
  .btn.btn-success New Download
%table.table.table-bordered
  %thead
    %td Name
    %td Opened
    %td Links
    %td Downloaded
  %tbody
    - @downloads.each do |download|
      %tr
        %td
          =link_to admin_dashboard_download_path(download) do
            %h5= download.name
        %td= download.download_links.count
        %td= download.download_links.where.not(first_access: nil).count
        %td= download.download_links.where(downloaded: true).count
