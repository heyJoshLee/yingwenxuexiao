%h1 Notifications Dashboard

= link_to new_admin_dashboard_notification_path do
  .btn.btn-primary New Notification

- Notification.sent_admin_notifications.each do |notification|
  %h3= notification[:subject]
  .well.default_formatting= notification[:body].html_safe
  %hr/
